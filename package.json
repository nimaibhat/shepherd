{
  "name": "shepherd-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Terminal-first control plane for persistent, cloud-hosted AI coding agents",
  "license": "MIT",
  "author": "Nimai Bhat",
  "repository": {
    "type": "git",
    "url": "https://github.com/nimaibhat/shepherd.git"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
